# 隐私权规范 - Chrome Web Store 发布说明

## 单一用途说明

**本扩展程序的单一用途：**
本扩展程序旨在为用户提供便捷的方式，通过右键菜单、快捷键或插件图标，将网页上选中的文本快速发送到DeepSeek AI助手进行搜索、翻译、解释等操作。扩展程序仅作为用户与DeepSeek服务之间的桥梁，不收集、存储或传输任何用户数据。

---

## 远程代码使用说明

**需使用远程代码的理由：**

本扩展程序需要在 `https://chat.deepseek.com/*` 域名下注入内容脚本（content script），以实现以下核心功能：

1. **自动填充输入框**：当用户通过扩展程序选择文本后，自动将文本填充到DeepSeek聊天页面的输入框中，提升用户体验。

2. **页面交互**：检测DeepSeek页面的输入框元素，确保文本能够正确填充到正确的位置。

3. **备用方案**：如果自动填充失败，将文本复制到剪贴板，用户可以手动粘贴。

**代码来源：**
- 所有代码均包含在扩展程序包内，不包含任何外部动态加载的代码
- Content script仅在用户主动触发操作时执行
- 代码完全开源，可在GitHub仓库中查看：https://github.com/eppen/deepseek-search-extension

---

## 主机权限使用说明

**需使用主机权限的理由：**

本扩展程序需要访问 `https://chat.deepseek.com/*` 域名，原因如下：

1. **核心功能实现**：扩展程序的主要功能是将用户选中的文本发送到DeepSeek AI助手。为了实现这一功能，需要在DeepSeek页面注入内容脚本。

2. **用户主动操作**：只有在用户明确选择文本并触发扩展程序操作时，才会访问DeepSeek网站。扩展程序不会在后台自动访问任何网站。

3. **最小权限原则**：权限范围严格限制在 `https://chat.deepseek.com/*` 域名，不会访问其他网站。

---

## activeTab 权限使用说明

**需使用 activeTab 权限的理由：**

1. **获取选中文本**：当用户使用快捷键（Alt+D）或点击插件图标时，需要读取当前活动标签页中用户选中的文本内容。

2. **用户主动操作**：仅在用户明确触发操作时访问当前标签页，不会在后台访问其他标签页。

3. **不存储数据**：获取的文本仅用于传递给DeepSeek服务，不会在本地存储或传输给第三方。

---

## contextMenus 权限使用说明

**需使用 contextMenus 权限的理由：**

1. **右键菜单功能**：扩展程序的核心功能之一是在用户选中文本后，在右键菜单中显示"DeepSeek"选项，提供搜索、翻译、解释等多种操作。

2. **提升用户体验**：通过右键菜单，用户可以快速访问DeepSeek功能，无需打开新标签页手动输入。

3. **仅显示菜单**：扩展程序仅创建菜单项，不会读取或访问用户未主动选择的内容。

---

## scripting 权限使用说明

**需使用 scripting 权限的理由：**

1. **获取选中文本**：当用户使用快捷键时，需要通过脚本注入获取当前页面中用户选中的文本。

2. **填充DeepSeek输入框**：在DeepSeek页面中，需要通过脚本自动检测并填充输入框，实现自动化操作。

3. **用户主动触发**：所有脚本注入操作均由用户主动触发，不会在后台自动执行。

4. **最小化影响**：脚本仅在必要时执行，执行完成后立即结束，不会持续运行。

---

## storage 权限使用说明

**需使用 storage 权限的理由：**

1. **未来功能扩展**：虽然当前版本未使用存储功能，但为未来可能的功能（如用户偏好设置、历史记录等）预留接口。

2. **可选功能**：如果未来需要保存用户的搜索偏好或常用操作，将使用本地存储，数据仅存储在用户本地设备，不会上传到任何服务器。

**当前状态**：当前版本未实际使用storage权限，所有数据均为临时处理，不会持久化存储。

---

## 数据使用情况说明

### 数据收集
**本扩展程序不收集任何用户数据。**

### 数据传输
1. **用户选中的文本**：仅在用户主动操作时，将文本发送到DeepSeek官方服务（https://chat.deepseek.com/）。这是扩展程序的核心功能，用户明确知晓并同意此操作。

2. **不传输给第三方**：除了DeepSeek官方服务外，不会将任何数据发送给其他第三方服务。

### 数据存储
1. **本地存储**：当前版本不使用任何本地存储功能。

2. **服务器存储**：扩展程序本身不运行任何服务器，不存储任何用户数据。

### 数据安全
1. **HTTPS传输**：所有与DeepSeek服务的通信均通过HTTPS加密连接进行。

2. **最小权限**：扩展程序仅请求必要的权限，遵循最小权限原则。

3. **开源透明**：所有代码开源，用户可随时审查代码，确保无恶意行为。

---

## 用户隐私保护

1. **无追踪**：扩展程序不包含任何追踪代码、分析工具或广告SDK。

2. **无后台活动**：扩展程序不会在后台运行或访问网页，仅在用户主动操作时执行。

3. **无数据收集**：不收集用户的浏览历史、个人信息或任何其他数据。

4. **用户控制**：用户可以随时禁用或卸载扩展程序，所有操作均由用户主动触发。

---

## 符合开发者计划政策

本扩展程序完全符合Chrome Web Store开发者计划政策：

✅ **单一用途**：扩展程序专注于提供DeepSeek搜索功能  
✅ **用户隐私**：不收集、不存储、不传输用户数据  
✅ **透明性**：代码完全开源，可公开审查  
✅ **最小权限**：仅请求必要的权限  
✅ **用户控制**：所有操作由用户主动触发  

---

## 联系方式

如有任何关于隐私或数据使用的疑问，请通过以下方式联系：

- GitHub Issues: https://github.com/eppen/deepseek-search-extension/issues
- 仓库地址: https://github.com/eppen/deepseek-search-extension

---

**最后更新日期：** 2026年2月1日  
**版本：** 1.2.0